# Production environment variables for GitHub Pages deployment
# These values are set via GitHub repository variables in the deployment workflow

# Backend URL - set to your Cloud Run service URL after first deployment
# Example: https://slop-video-backend-abc123-uc.a.run.app
VITE_BACKEND_URL=https://YOUR-CLOUD-RUN-URL.run.app

# Base path for GitHub Pages (if using a project site, not user/org site)
# Example: /sloper for https://username.github.io/sloper
# Leave as / for user/org sites or custom domains
VITE_BASE_PATH=/

# Google Drive OAuth Client ID (optional — enables "Upload to Google Drive" button)
# Create at: https://console.cloud.google.com/apis/credentials
VITE_GOOGLE_CLIENT_ID=
